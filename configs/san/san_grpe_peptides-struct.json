{
  "_comment": "SAN config: PE=grpe, dataset=peptides-struct. Generated by scripts/generate_san_configs.py from backends/san_backend.py's PE_SPEC / BASE_NET_PARAMS / TRAIN_PARAMS -- do not hand-edit, re-run the generator after changing those. NOT upstream-tuned: SAN's own repo ships no LRGB reference configs (see san_backend.py's module docstring, consequence 1).",
  "pe": "grpe",
  "dataset": "peptides-struct",
  "params": {
    "epochs": 200,
    "batch_size": 32,
    "init_lr": 0.0007,
    "lr_reduce_factor": 0.5,
    "lr_schedule_patience": 10,
    "min_lr": 1e-06,
    "weight_decay": 0.0
  },
  "net_params": {
    "lpe_enable": false,
    "grpe_bias_enable": true,
    "grpe_num_spd_buckets": 24
  }
}
